<!-- SPDX-License-Identifier: Apache-2.0 -->

# 调试配套材料

tool-labs.code-workspace 保存正文完整展示的构建任务与 GDB 启动配置；inspect.gdb 保存同一观察过程的命令行版本。工作区引用 CMake 的起始原件，使用独立产物目录 cmake-host。

从仓库根打开本目录的工作区文件即可，正文按配置、断点、单步、观察的顺序讲解。此处不另设一套实验步骤。
